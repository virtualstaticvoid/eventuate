require "spec_helper"

describe Eventuate do
  it "has a version number" do
    expect(Eventuate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
